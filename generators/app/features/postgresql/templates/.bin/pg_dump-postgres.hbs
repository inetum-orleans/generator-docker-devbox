#!/usr/bin/env sh
. "$DOCKER_DEVBOX_DIR/.bash.lib.d/50-docker"

run --user postgres --workdir="$(_docker_workdir "/workdir")" {{service.name}} pg_dump --dbname=postgres://{{projectName}}:{{projectName}}@{{service.name}}/{{projectName}} "$@"
