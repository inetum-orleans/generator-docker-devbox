#!/usr/bin/env sh
. "$DOCKER_DEVBOX_DIR/.bash.lib.d/docker"

run --user postgres --workdir="$(_docker_workdir "/workdir")" db pg_dump --dbname=postgres://{{projectName}}:{{projectName}}@{{service.name}}/{{projectName}} "$@"
