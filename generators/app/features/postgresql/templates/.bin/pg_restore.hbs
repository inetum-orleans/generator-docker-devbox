#!/usr/bin/env bash
. "$DOCKER_DEVBOX_DIR/.bash.lib.d/50-docker"

run --user postgres --workdir="$(_docker_workdir "/workdir")" {{instance.name}} pg_restore --dbname=postgres://{{projectName}}:{{projectName}}@{{instance.name}}/{{projectName}} "$@"
