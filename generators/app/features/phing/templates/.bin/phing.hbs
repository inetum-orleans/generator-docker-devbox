#!/usr/bin/env bash
. "$DOCKER_DEVBOX_SCRIPTS_PATH/.bash.lib.d/50-docker"

run --user www-data --entrypoint php --workdir="$(_docker_workdir "/app")" {{instance.name}} /composer/vendor/phing/phing/bin/phing "$@"
