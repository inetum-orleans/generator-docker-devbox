#!/usr/bin/env sh
. "$DOCKER_DEVBOX_DIR/.bash_enter.d/03-functions"

#TODO: This won't work ... Check postgreSQL script for example
dc exec --user root {{service.name}} mysql -u{{projectName}} -p "$@"
