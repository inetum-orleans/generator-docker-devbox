#!/usr/bin/env bash

# Cleanup .bash_enter.d/05-variables
unset USER_ID
unset GROUP_ID
unset USER_NAME
unset GROUP_NAME

unset COMPOSE_PROJECT_DIR
unset COMPOSE_PROJECT_NAME
unset COMPOSE_NETWORK_NAME

unset DOCKER_DEVBOX_DIR
unset DOCKER_VERSION
unset DOCKER_COMPOSE_VERSION

unset DOCKER_DEVBOX_ENV
unset DOCKER_DEVBOX_ENV_LIST

# Cleanup .bash_enter.env
unset DOCKER_DEVBOX_DOMAIN
unset DOCKER_DEVBOX_DOMAIN_PREFIX
unset DOCKER_DEVBOX_PORT_PREFIX
unset DOCKER_DEVBOX_RESTART_POLICY
unset DOCKER_DEVBOX_ENV
unset DOCKER_DEVBOX_ENV_LIST
unset DOCKER_DEVBOX_ENV_SYMLINKS
unset DOCKER_DEVBOX_MO_DIRS

#unset DOCKER_DEVBOX_REGISTRY
#unset DOCKER_DEVBOX_CA_CERTIFICATES
